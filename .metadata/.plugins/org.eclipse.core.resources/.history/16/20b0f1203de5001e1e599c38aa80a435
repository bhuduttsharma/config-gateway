package net.javaguides.school.JpaRepository;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;

import net.javaguides.school.Entity.School;

public interface StudentRepository extends JpaRepository<School, Integer> {
	List<School> findAllBySchoolId(Integer schoolId);
}
